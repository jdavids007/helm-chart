apiVersion: v1
kind: Service
metadata:
  name: {{ include "cps-preference-api.fullname" . }}
  labels:
    {{- include "cps-preference-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cps-preference-api.selectorLabels" . | nindent 4 }}
